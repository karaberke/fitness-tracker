# Loaded by Vitest (mode=test) on top of `.env`. Points at a throwaway database
# in the same Docker Postgres so tests never touch development data.
DATABASE_URL="postgres://root:mysecretpassword@localhost:5432/fitness_test"
BETTER_AUTH_SECRET="test-secret-not-for-production"
